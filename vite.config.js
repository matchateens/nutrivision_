import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  base: './', // Menggunakan path relatif agar asset (JS/CSS) terbaca dengan benar di GitHub Pages
});
